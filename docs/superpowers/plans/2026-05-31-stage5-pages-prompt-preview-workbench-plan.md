# Stage5Pages 中间提示词工作台与右侧预览区重设计 — 实现计划

**日期**: 2026-05-31
**设计文档**: `docs/superpowers/specs/2026-05-31-stage5-pages-prompt-preview-workbench-design.md`

---

## 实施步骤

### Step 1: 梳理当前渲染块并补充派生显示数据

**文件改动**：
1. `src/components/studio/stages/Stage5Pages.tsx`

**改动内容**：
- 在不改变现有状态结构的前提下，整理当前选中目标的派生显示字段
- 增加右侧预览头部、历史记录骨架所需的展示数据
- 为“历史记录”构造兼容当前结构的占位数据，只基于当前 `imageUrl`

**验证**：
- 不改变现有生成逻辑
- TypeScript 类型保持正确

---

### Step 2: 重构中间区为三张工作台卡片

**文件改动**：
1. `src/components/studio/stages/Stage5Pages.tsx`

**改动内容**：
- 保留当前页标题和状态 badge
- 将 `画面文字` 与 `画面内容描述` 合并为一张内容输入卡
- 将 `AI 绘画提示词` 单独强化为核心卡片
- 将 `引用角色` 与 `引用场景` 合并为统一素材引用卡
- 收短说明文案，优化标题、边框、留白、背景层次

**验证**：
- 中间区仍能编辑相同字段
- `PromptEditor`、错误提示、重新生成提示词按钮仍正常工作

---

### Step 3: 重构右侧为预览优先结构

**文件改动**：
1. `src/components/studio/stages/Stage5Pages.tsx`

**改动内容**：
- 将右侧改为 `主预览卡 + 历史记录带 + 操作卡 + 进度卡`
- 在主预览卡中加入当前页名、状态、比例等轻量信息
- 空状态、生成中、已生成三种状态统一视觉表达
- 保留点击大图放大预览

**验证**：
- 右侧当前图仍可正常预览
- 生成中态、空态和已有图片态都可正确显示

---

### Step 4: 接入历史记录骨架

**文件改动**：
1. `src/components/studio/stages/Stage5Pages.tsx`

**改动内容**：
- 增加横向历史缩略图区
- 当前阶段先基于当前 `imageUrl` 渲染一个“当前版本”缩略卡
- 无图时显示空状态提示
- 不扩展页面或封面的历史数组结构

**验证**：
- 有图时历史区可见且有高亮项
- 无图时历史区仍有合理结构，不塌陷

---

### Step 5: 回归检查与诊断

**文件改动**：
1. `src/components/studio/stages/Stage5Pages.tsx`

**改动内容**：
- 检查桌面端三栏和移动端堆叠是否正常
- 运行诊断检查 JSX / TypeScript 问题
- 如有明显样式结构问题，做小范围修正

**验证**：
- `GetDiagnostics` 无新增错误
- 现有主要交互仍保持可用

---

## 回归检查清单

- [ ] 页面切换仍正常
- [ ] 封面和正文页都能编辑文字与画面描述
- [ ] 提示词自动生成和手动重生成仍正常
- [ ] `PromptEditor` 编辑不受布局改动影响
- [ ] 当前图点击后仍可弹窗预览
- [ ] 生成按钮、重新生成按钮、进入编辑页按钮仍正常
- [ ] 右侧历史记录在无真实历史数组时仍有稳定 UI 骨架
- [ ] 不引入新的 TypeScript 或 JSX 诊断错误
